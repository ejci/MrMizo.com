<!DOCTYPE html>
<html lang="en">
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<meta name="description" content="<%= meta.description %>" />
		<meta name="keywords" content="<%= meta.keyword %>" />
		<meta name="author" content="<%= meta.author %>" />
		<title><%= meta.title %></title>
		<link href="//maxcdn.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css" rel="stylesheet">
		<link href='http://fonts.googleapis.com/css?family=Cabin+Sketch:700' rel='stylesheet' type='text/css'>
		<link href='http://fonts.googleapis.com/css?family=Coming+Soon' rel='stylesheet' type='text/css'>
		<link rel="stylesheet" href="/css/style.css" />
		<link rel="icon" type="image/png" href="favicon.png" />

		<meta property="og:title" content="<%= meta.title %>"/>
		<meta property="og:type" content="website"/>
		<meta property="og:url" content="http://mrmizo.com"/>
		<meta property="og:description" content="<%= meta.description %>"/>
		<meta property="og:image" content="http://mrmizo.com/og-image-app.png"/>
		<meta property="og:image:type" content="image/png"/>
		<meta property="og:image:width" content="160"/>
		<meta property="og:image:height" content="96"/>
	</head>

	<body class="" style="display:none">
		<div class="masthead clearfix hidden-object">
			<div class="inner">
				<h3 class="masthead-brand"><%= home.name %></h3></a>
				<ul class="nav masthead-nav">
					<li>
						<a href="#home">Home</a>
					</li>
					<li>
						<a href="#about">About</a>
					</li>
					<li>
						<a href="#products">Products</a>
					</li>
				</ul>
			</div>
		</div>
		<div class="site-wrapper home" id="home">
			<div class="site-wrapper-inner">
				<div class="cover-container">
					<div class="inner cover logo">
						<h1 class="cover-heading">
							<% for(var i=0; i<home.name.length; i++) {%><span class="hidden-object"><%= home.name[i] %></span><% } %>
						</h1>	
						<p class="lead hidden-object">
							<%= home.tagline %>
						</p>
						<p class="lead hidden-object">
							<% for(var i=0; i<home.buttons.length; i++) {%>
								<a href="<%= home.buttons[i].url %>" class="btn btn-lg btn-default"><%= home.buttons[i].name %></a>
							<% } %>
						</p>
					</div>
				</div>

			</div>

		</div>
		<div class="site-wrapper about" id="about">
			<div class="site-wrapper-inner">
				<div class="cover-container">
					<div class="inner cover">
						<h1 class="cover-heading"><%= about.heading %></h1>
						<% for(var i=0; i<about.content.length; i++) {%>
							<p class="lead"><%= about.content[i] %></p>
						<% } %>
						<p class="lead">
							<% for(var i=0; i<about.buttons.length; i++) {%>
								<a href="<%= about.buttons[i].url %>" class="btn btn-lg btn-primary"><%= about.buttons[i].name %></a>
							<% } %>
						</p>
					</div>
				</div>
			</div>
		</div>
		<!--PRODUCTS START-->
		<div class="site-wrapper products" id="products">
			<div class="site-wrapper-inner">
				<div class="cover-container">
					<div class="inner cover">

						<% for(var row=0; row<products.length; row=row+3) {%>
						<div class="row">
							<% for(var i=0; i<3; i++) {%>
								<%if(products[row+i]){%>
								<div class="col-lg-4 product">
									<img class="img-circle" src="products/<%= products[row+i].image %>">
									<a href="<%= products[row+i].link %>" title="<%= products[row+i].name %>"><h3><%= products[row+i].name %></h3></a>
									<small><%= products[row+i].desc %></small>
									<small><%= products[row+i].material %></small>
									<small><%= products[row+i].color %></small>
									<small><%= products[row+i].size %></small>
								</div>
								<% } %>
							<% } %>
						</div>
						<% } %>

					</div>
				</div>

			</div>

		</div>
		<!--PRODUCTS END-->
		<script src="//code.jquery.com/jquery-1.11.1.min.js"></script>
		<script src="//maxcdn.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
		<script src="js/logo.js"></script>
		<script>
			(function(i, s, o, g, r, a, m) {
				i['GoogleAnalyticsObject'] = r;
				i[r] = i[r] ||
				function() {
					(i[r].q = i[r].q || []).push(arguments);
				}, i[r].l = 1 * new Date();
				a = s.createElement(o), m = s.getElementsByTagName(o)[0];
				a.async = 1;
				a.src = g;
				m.parentNode.insertBefore(a, m);
			})(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');

			ga('create', 'UA-42932229-1', 'mrmizo.com');
			ga('send', 'pageview');
		</script>
	</body>
</html>
